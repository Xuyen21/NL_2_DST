install promptfoo:
npm install
npm run eval

install litellm:

Optional: `erdantic` is not part of the default install. On Windows it may fail with `Failed to build pygraphviz` unless Graphviz and its build prerequisites are installed. The app does not require it for the normal text-to-json or PlantUML flows.
