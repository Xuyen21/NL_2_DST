openai==2.28.0
instructor==1.14.5
pydantic==2.12.5
python-dotenv==1.2.2
sentence-transformers==5.2.3
streamlit
chromadb
requests
plantuml
uv==0.11.21
litellm==1.89.0
spacy
pytest


# erdantic is optional and not required for the main app.
# On Windows it pulls in pygraphviz, which needs native Graphviz tooling to build.
# Keep it out of the default install unless you specifically need schema diagram generation.